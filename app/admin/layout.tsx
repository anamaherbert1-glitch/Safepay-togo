import type { ReactNode } from "react";
import "./admin-polish.css";
import "./admin-module-polish.css";

export default function AdminLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
